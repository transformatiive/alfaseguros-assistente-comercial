{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "zoho:probe-tickets": "tsx ./src/zoho-probe-tickets.ts",
    "ringover:probe-users": "tsx ./src/ringover-probe-users.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workspace/ringover": "workspace:*",
    "@workspace/zoho-desk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
