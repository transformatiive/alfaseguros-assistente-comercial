# Tasks: Phase 2A — Zoho Desk Integration & Cross-Channel Cases

> Ordered. Each task is small enough to complete in one Claude Code session and testable on its own.

## 1. Wire up the outcome classifier

- [ ] 1.1 Run `npm run zoho:probe-tickets -- --days=14 --limit=30` against real Alfaseguros Desk
- [ ] 1.2 Walk the output with Nuno: identify which `cf_*` fields and values mean won / lost / open
- [ ] 1.3 Replace the heuristic block in `src/analysis/outcome.ts` with concrete rules (one rule per cf field/value combination, ordered most-specific first)
- [ ] 1.4 Each rule sets `reason` to a human-readable string referencing the field+value
- [ ] 1.5 Default fallback returns `unknown` (never guess)
- [ ] 1.6 Add unit tests covering: typical won, typical lost, typical open, ambiguous → unknown

## 2. Build the case-level analyzer

- [ ] 2.1 Create `src/analysis/case-analyzer.ts` with `analyzeCase(case: LinkedCase): Promise<AnalysisResult>`
- [ ] 2.2 Reuse `buildSystemPrompt()` from `src/analysis/prompts.ts` (procedures-aware)
- [ ] 2.3 Write a new `buildCaseUserMessage(case)` that walks `case.legs[]` chronologically and formats each by type (call → "Chamada Inbound/Outbound", ticket_event → "Ticket aberto", ticket_comment → "Comentário (channel, author, AGENT/END_USER)")
- [ ] 2.4 Strip Ringover summaries' `Generated by AI...` suffix; sanitize comment HTML to plain text; truncate each comment to 1500 chars
- [ ] 2.5 Token-budget guard: if total prompt > 30k tokens, summarize old comments individually first then re-issue
- [ ] 2.6 Validate output against `analysisSchema` (Zod); on failure, persist error and continue
- [ ] 2.7 Apply prompt caching to the system prompt (`cache_control: ephemeral`)

## 3. Persist cases

- [ ] 3.1 Add repo functions in `src/storage/repo.ts`: `saveCase`, `findCachedCase` (key = case.id), `getCasesForWindow(from, to, filters)`, `clearCaseAnalysis`
- [ ] 3.2 `saveCase` upserts the `Case` row plus its `CaseTicket` and `CaseCall` link rows
- [ ] 3.3 `findCachedCase` returns null if `analysisJson` is null
- [ ] 3.4 Add unit test: upsert is idempotent

## 4. Wire cases into the daily orchestrator

- [ ] 4.1 In `src/jobs/analyze-day.ts`, after step 6 (conversation summary), call `syncTickets(date - 90d, date)` if Zoho creds present
- [ ] 4.2 Then call `buildCases({ calls, tickets, comments })` and persist via `saveCase`
- [ ] 4.3 Run `analyzeCase` over the case set with same `PQueue(concurrency=4)` pattern as conversations
- [ ] 4.4 Cache-by-default: skip cases that already have `analysisJson`
- [ ] 4.5 Emit new SSE events: `case:start`, `case:done`, `case:error`, `cases:done`
- [ ] 4.6 Update Run row's cost totals to include case analysis costs

## 5. Pipeline API

- [ ] 5.1 Add `GET /api/day/:date/pipeline` that returns cases whose `lastActivityAt` falls within ±N days of the date (default N=14, configurable via query)
- [ ] 5.2 Support filters via query params: `outcome`, `product`, `agent`, `daysSinceTouchMin`
- [ ] 5.3 Sort by `lastActivityAt` descending by default
- [ ] 5.4 Add CaseDTO type in `src/ui/types.ts` matching the response shape

## 6. Pipeline view UI

- [ ] 6.1 Add "Pipeline" as a third option in the view toggle in `src/ui/App.tsx`
- [ ] 6.2 Create `src/ui/components/PipelineView.tsx` with a cards list (case-per-card)
- [ ] 6.3 Each card header: customer name (or phone if no name), agents, product badge, outcome badge (won/lost/open/unknown with colour coding), days-since-touch
- [ ] 6.4 Expanding the card shows the full case timeline (calls + ticket events + comments) and the case-level analysis
- [ ] 6.5 Filters above the list: outcome (multi-select), product, agent, "stale only" (>7 days no touch)
- [ ] 6.6 Match the existing UI aesthetic: warm neutrals, Georgia serif for narrative, color-coded section borders, no Tailwind utility-class chaos

## 7. End-to-end demo prep

- [ ] 7.1 Sync 90 days of tickets in production (`npm run zoho:sync-tickets -- --days=90` in Replit Shell)
- [ ] 7.2 Run a force re-analysis for yesterday so cases populate
- [ ] 7.3 Spot-check 5 cases: do narratives include email content correctly? Are outcomes plausibly classified?
- [ ] 7.4 Pick one case where multi-channel meaningfully changes the analysis vs call-only — this is the demo highlight for Rui
- [ ] 7.5 Take screenshots of pipeline view filtered to closed-lost TVDE cases — that's Rui's "why are we losing?" answer

## 8. Documentation update

- [ ] 8.1 Update `CLAUDE.md` Phase 2A section: change wording from "in progress" to "shipped"
- [ ] 8.2 Update `HANDOVER.md` punt list: move Phase 2A items to "shipped"; surface Phase 2B items
- [ ] 8.3 Move this folder to `openspec/changes/archive/phase-2a-desk-cases/`
- [ ] 8.4 Merge the delta from `specs/supervisor/spec.md` (in this change folder) into the canonical `openspec/specs/supervisor/spec.md`

## 9. Verification

- [ ] 9.1 All scenarios in this change's `specs/supervisor/spec.md` (delta) pass manual test
- [ ] 9.2 23+ existing unit tests still pass; new tests for outcome classifier and `saveCase` pass
- [ ] 9.3 `npx tsc --noEmit` clean; `npx vite build` clean
- [ ] 9.4 Daily cron fires successfully at 07:00 next workday and produces a complete pipeline view
